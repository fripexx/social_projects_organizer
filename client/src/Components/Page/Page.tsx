import React, {FC} from 'react';
import classes from "./Page.module.scss";

const Page: FC = () => {
    return (
        <div className={classes.page}>

        </div>
    );
};

export default Page;